#!/bin/sh

export GPG_TTY="$(tty)"

PINENTRY_PROGRAM="--pinentry-program=${PINENTRY_PROGRAM:-/usr/bin/pinentry-curses}"

if [ "$#" -eq 0 ]; then
    exec gpg-agent "$PINENTRY_PROGRAM" --daemon "${SHELL:-$(command -v bash)}"
else
    exec gpg-agent "$PINENTRY_PROGRAM" --daemon "$@"
fi
