#!/bin/sh

if test -x /usr/bin/open; then
    exec /usr/bin/open "$@"
elif [[ -x /Applications/FirefoxAurora.app/Contents/MacOS/firefox-bin ]]; then
    /Applications/FirefoxAurora.app/Contents/MacOS/firefox-bin "$@"
else
    firefox-bin "$@"
fi
