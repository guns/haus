#!/usr/bin/env ruby
# -*- encoding: utf-8 -*-

ARGV.each do |f|
  case url = File.read(f).chomp
  when %r{\A\w+://}
    system 'open', url
  when 'about:preferences', 'about:config'
    system 'firefox', url
  end
end
