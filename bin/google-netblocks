#!/usr/bin/env ruby
# -*- encoding: utf-8 -*-

require 'resolv'

puts Resolv::DNS.open { |dns|
  dns.getresource('_netblocks.google.com', Resolv::DNS::Resource::IN::TXT).data
}.scan(/ip4:(\S+)/).join("\n")
