#!/usr/bin/env python

import sys

import haus

haus.execvp(
    [
        "yt-dlp",
        "--output",
        "%(title)s-%(id)s.%(ext)s",
        "--trim-filenames=120",
        "--user-agent",
        haus.get_random_user_agent("Linux"),
        *sys.argv[1:],
    ]
)
