#!/bin/sh

if test -n "$SSH_AGENT_PID"; then
    echo "SSH_AGENT_PID already set: $SSH_AGENT_PID"
elif test "$#" -gt 0; then
    run ssh-agent "$@"
else
    run ssh-agent "${SHELL:-$(command -v bash)}"
fi
