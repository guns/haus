#!/usr/bin/env ruby
# -*- encoding: utf-8 -*-

Dir['/sys/block/*/dm'].each do |dm|
  puts '%s: %s' % [
    File.read("#{dm}/name").chomp,
    File.read("#{dm}/suspended").chomp == '1' ? 'suspended' : 'active'
  ]
end
