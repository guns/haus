#!/usr/bin/env bash

vimrepl() {
    local file cmd

    case $# in
    0) file='.vimrepl';;
    1) file='.vimrepl' cmd="$1";;
    *) file="$1" cmd="${*:2}";;
    esac

    exec vim -c "Screen $cmd" "$file"
}

vimrepl "$@"
