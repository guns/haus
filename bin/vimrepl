#!/usr/bin/env bash

vimrepl() {
    local file cmd

    case $# in
    2) cmd="$1" file="$2";;
    1) cmd="$1" file='.vimrepl';;
    0) file='.vimrepl';;
    *) return 1
    esac

    exec vim -c "Screen $cmd" "$file"
}

vimrepl "$@"
