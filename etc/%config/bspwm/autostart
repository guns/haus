#!/bin/bash
# X settings should be in ~/.xinitrc

. ~/.bashrc.d/env.bash
. ~/.bashrc.d/path.bash

pushd .
cd Downloads

pulseaudio --start &
(/bin/sh ~/.fehbg; exec /usr/bin/tint2) &
/usr/bin/kupfer --no-splash &
xautolock-wrapper &

popd
