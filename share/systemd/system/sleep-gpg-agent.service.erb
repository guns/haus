[Unit]
Description=Send SIGHUP to gpg-agent processes on suspend
Before=sleep.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c '\
	if <%= path 'pgrep' %> -x gpg-agent >/dev/null; then \
		<%= path 'pkill' %> -x -HUP gpg-agent; \
	fi'

[Install]
WantedBy=sleep.target
